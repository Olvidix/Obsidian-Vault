Administración remota de Windows ( `WinRM`) es un protocolo simple de administración remota integrado en Windows basado en la línea de comandos.

```
nmap -sV -sC [IP] -p5985,5986 --disable-arp-ping -n
```

Para conectarnos al servicio:

```
evil-winrm -i [IP] -u [USUARIO] -p '[CONTRASEÑA]'
```

